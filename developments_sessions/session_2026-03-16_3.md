# Сессия 2026-03-16 #3 — Factchecker, WordPress интеграция

## Что сделано

### 1. Скилл `/factchecker`
- Создан `.claude/skills/factchecker/SKILL.md`
- Два метода проверки:
  - **Верификация источников** — загружает URL из `sources_used` в frontmatter, сравнивает факты с оригиналами
  - **Перекрёстная проверка** — WebSearch по ключевым фактам, сверка с альтернативными источниками
- Флаги: `--deep` (расширенный поиск), `--lang` (язык запросов)
- Оценка достоверности: ВЫСОКАЯ / СРЕДНЯЯ / НИЗКАЯ / КРИТИЧЕСКАЯ
- Предлагает исправления, но не вносит без подтверждения

### 2. WordPress интеграция (два домена)
- Создан `.env` на базе `.env.example` с доменными префиксами:
  - `WOWCASUAL_WORDPRESS_*` → wowcasual.info
  - `TECHNOCRATS_WORDPRESS_*` → technocrats.com.ua
- Обновлён `newsroom/publisher/config.py`:
  - Добавлен `DOMAIN_PREFIXES` маппинг
  - `WordPressConfig.from_env(domain)` — читает креды по префиксу домена
  - Обратная совместимость: без домена ищет `WORDPRESS_*`
- Обновлён `newsroom/publisher/wordpress.py`:
  - `WordPressPublisher(domain="wowcasual")` — принимает домен

### 3. Тестирование публикации
- Application Password создан для Deckven на wowcasual.info
- **Проблема**: nginx на хостинге блокирует POST к `/wp-json/` (405 Not Allowed)
- GET работает (200), POST — 405 от nginx (не от WordPress)
- curl подтвердил: при правильной авторизации WP отвечает 401 "rest_cannot_create", но nginx перехватывает POST раньше

## Что нужно сделать дальше

### WordPress (блокер)
- Разрешить POST/PUT/DELETE к `/wp-json/` в nginx-конфиге хостинга:
  ```nginx
  location /wp-json/ {
      try_files $uri $uri/ /index.php?$args;
  }
  ```
- Или альтернатива: XML-RPC (`xmlrpc.php`) вместо REST API
- После исправления nginx — повторить тест публикации

### Дальнейшие шаги по блогам
- Конвертация Markdown → HTML перед публикацией
- Маппинг категорий/тегов (автосоздание через API)
- Настроить креды для technocrats.com.ua
